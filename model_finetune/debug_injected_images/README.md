# 📂 訓練增強調試影像目錄 (Debug Injected Images Directory)

本資料夾存放了在模型訓練過程中，資料增強（Data Augmentation）或背景替換流程中生成的對比影像，用於開發與調試。

---

## 📄 影像說明
* **`orig_*.png`**：原始影像，保留原本拍攝時的原始光線與背景。
* **`injected_*.png`**：注入增強後的影像（例如：人工替換背景、注入雜訊、對比度調整或旋轉平移後），用以觀察資料前處理管線（Data Pipeline）是否正常工作，並確認模型吃到的特徵是否會因前處理而失真。
